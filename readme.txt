Libraries to install:
1. react-router-dom
2. flask

To install all needed libraries;
1. cd {path to get inside project}
2. npm install react-router-dom
3. pip install flask

to run the code:
1. cd {path to get inside backend folder}
2. python -m venv venv
3. for windows: venv\Scripts\activate
   for macOS/Unix: source venv/bin/activate
4. pip install flask
5. pip install flask-cors
6. python app.py
7. npm start